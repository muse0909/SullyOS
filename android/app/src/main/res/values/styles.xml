<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- 暮色 2026-09-09（第五次修正 splash）：换暮色给的新 splash 图（1254x1254 方形）
         https://i.ibb.co/PGc3K1YG/tb2-1.png（深蓝月夜风 + 月亮 + 牵牛星 + 小女孩 + 猫）
         Android 12+ SplashScreen API：背景色 #b8d4ec + 居中图标（splash 图 ~108dp）
         Android 5-11 用 android:background 兜底（全屏背景图）
         暮色 21:43 给的新图替代之前的浅蓝拱窗（暮色之前看到"压缩成很小一个"就是 SplashScreen API 居中 icon）。 -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">#b8d4ec</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>