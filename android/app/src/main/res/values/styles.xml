<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- 暮色 2026-09-10（第六次修正 splash）：换暮色 21:43 给的 tb2-1.png（1254x1254 方形圆角深蓝月夜）
         https://i.ibb.co/PGc3K1YG/tb2-1.png
         Android 12+ SplashScreen API：白色背景 #ffffff + 居中图标（tb2-1.png ~108dp）
         Android 5-11 用 android:background 兜底（全屏背景图）
         android:windowBackground=#ffffff 让 splash 关掉那一帧 WebView 默认底也是白色（无白闪）
         暮色 9-10 22:17 明确要求"背景要白色，不要浅蓝"
         加载页（loading-default.png = 1233455.jpg 长方形浅蓝拱窗）保持不动 -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowBackground">#ffffff</item>
        <item name="windowSplashScreenBackground">#ffffff</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>