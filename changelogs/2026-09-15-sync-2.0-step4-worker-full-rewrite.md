## sync 2.0 step 4：完整搬 upstream Worker 端主动消息 2.0

按暮色 9-15 12:19 拍板，把上游 Cloudflare Worker 端主动消息 2.0 整文件覆盖。
Worker 端跟前端解耦（用 esbuild 单独打 bundle.js），不影响前端 Vite build。

### 改动清单

**Worker 端整文件覆盖：**

| 文件 | upstream 行数 | SullyOS 之前 | 干什么 |
|---|---|---|---|
| `worker/amsg/src/index.ts` | 3140 | 118 | Worker 主入口（HTTP API + cron + DurableObject 调度）|
| `worker/amsg/src/realtimeWorld.ts` | 246 | — | 实时世界 block 构造 |
| `worker/amsg/src/selfUpdate.ts` | 405 | — | Worker 自更新（保留密钥）|
| `worker/amsg/src/cronTrigger.ts` | 168 | — | cron 触发读 / 写 / 鉴权失败判定 |
| `worker/amsg/src/nativeFcm.ts` | 161 | — | 原生 FCM 推送装配 |
| `worker/amsg/src/agentic.ts` | 517 | — | agentic tool dispatch |
| `worker/amsg/src/fireKinds.ts` | 129 | — | fire kind 处理器接口 |
| `worker/amsg/src/instantChat.ts` | 558 | — | 即时对话 DurableObject |
| `worker/amsg/src/plateFire.ts` | 161 | — | 牌局 fire 子模块 |

**新建子目录：**

| 路径 | 行数 | 干什么 |
|---|---|---|
| `worker/instant-push/src/classifier.ts` | 424 | 即时推送分类器（worker 端引用）|

**utils 整文件覆盖 / 新增：**

| 文件 | upstream 行数 | SullyOS 之前 | 干什么 |
|---|---|---|---|
| `utils/amsgFireSchedule.ts` | 476 | 303 | fire schedule 工具链（整覆盖，多 6 个 export：AMSG_FIRE_CANCEL_TOOL / AMSG_FIRE_RENEW_TOOL / buildFireCancelTool / buildFireRenewTool / buildSelfScheduleUuid 等）|
| `utils/realtimeWorldCore.ts` | 508 | — | 实时世界核心 |
| `utils/transferFormat.ts` | 317 | — | 转账格式解析 |
| `utils/timeFramingNote.ts` | 17 | — | 「现在几点」语境框定常量 |

**配置变更：**

- `package.json`：`@cloudflare/workers-types` 5.20260915.1 加到 devDependencies（DurableObject / Env 类型用）
- `scripts/build-workers.mjs`：amsg worker 加 `external: ['cloudflare:workers']`（CF workerd 运行时提供的虚拟模块）
- `tsconfig.json`：exclude `worker`（让前端 tsc 不扫 worker 子模块）
- `tsconfig.worker.json`：新增 worker 端 tsconfig（lib=ES2022, types=[@cloudflare/workers-types]）
- `worker/types-shim.d.ts`：新增 ambient 声明，兼容 SullyOS utils/ 里浏览器 API（localStorage 等）在 worker tsc 下的类型检查

### 摸底

- **Worker build：** ✓ `worker/amsg/worker.bundle.js` 625.7kb（CF free tier 1MB 内）
- **Proactive-push build：** ✓ 23.6kb（没破坏现有 1.0 push 加速器）
- **前端 Vite build：** ✓ 4.50s（worker 改动不影响前端）
- **前端 tsc 错数：** 538（baseline，跟 worker 改动无关）
- **Worker tsc 错数：** 86（剩下是 utils/ 浏览器 API 类型错，esbuild 不看 tsc，运行时 workerd 验证）

### 部署注意

- Worker bundle 已准备好，但**没部署到 Cloudflare**——部署需要 Cloudflare 账号 + wrangler 配置 + D1 database_id + 4 个 Secret，跟暮色 8-6 wrangler.toml 流程配套
- **不部署不影响手机**（手机跑前端，跟 Worker 端解耦）
- 想测试主动消息 2.0 推送链路时再部署

### 未触动

- Android 端 UnifiedPush（plan step 5 暂放）—— upstream 没 android/ 目录，FCM 集成需要 Firebase 项目
- Worker 部署（需要 Cloudflare 账号，下次再说）