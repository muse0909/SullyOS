<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- 暮色 2026-09-09：加 Android 12+ SplashScreen API 兼容
         之前只配 android:background（Android 5-11 生效），12+ 用了新 SplashScreen API，
         不认 android:background 字段，所以 splash 显示成默认白底（暮色看到"先白屏再蓝闪"）。
         加 windowSplashScreenBackground（背景色，跟 loading 图底色 #b8d4ec 一致防止闪黑）
         + windowSplashScreenAnimatedIcon（icon 居中显示 splash 图），
         同时保留 android:background 给旧 Android 兜底。 -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="windowSplashScreenBackground">#b8d4ec</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>