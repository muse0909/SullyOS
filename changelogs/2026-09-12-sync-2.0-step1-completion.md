# 2026-09-12 归档：主动消息 2.0 同步第 1 步补完 — 11 个依赖 + 装 1 个包 + 4 个二次依赖

## 范围
- **base**: `34a3a5e1`（30 个新文件已就位 + 摸底）
- **scope**: 补完 11 个 SullyOS 缺的依赖 + 装 1 个 npm 包 + 4 个二次依赖

## 补完的文件（11+4=15 个）

### 8 个 2.0 必需支撑（搬运 11 → 实际 15 个 + 装 1 个包）
| 文件 | 行数 | 用途 |
|---|---|---|
| `utils/mcpFireCore.ts` | 906 | MCP 工具调用核心（22 个新 utils 间接依赖）|
| `utils/agenticTools.ts` | 838 | OpenAI tool calling 工具定义 |
| `utils/memoryPalace/roomPlateCloud.ts` | 547 | 记忆宫殿云端同步 |
| `utils/memoryPalace/roomPlateCore.ts` | 447 | 记忆宫殿核心 |
| `utils/scheduleChange.ts` | 203 | 日程表改动 |
| `utils/buildInfo.ts` | 23 | 构建信息 |
| `worker/amsg/src/emotionEval.ts` | 209 | 情绪评估 worker |
| `utils/realtimeFetchCore.ts` | 473 | 实时 fetch 核心（agenticTools 依赖）|
| `utils/scheduleChangeParse.ts` | 131 | 日程表解析 |
| `utils/dailySchedule.ts` | 64 | 日程表日常模式 |
| `utils/emotionEvalCore.ts` | 155 | 情绪评估核心（emotionEval 依赖）|

### 1 个 npm 包
| 包 | 版本 | 说明 |
|---|---|---|
| `@capacitor/push-notifications` | `^6.0.5` | 装 `^6` 兼容 SullyOS Capacitor 6 生态（upstream 用 8.x，但 SullyOS 是 6.x，强行装 8.x 会破坏 6 生态）。**AndroidManifest.xml 权限差异**留到第 5 步处理 |

### 1 个文件没动
| 文件 | 原因 |
|---|---|
| `utils/realtimeContext.ts` | SullyOS 已有（1865 行 vs upstream 1625 行），SullyOS 版本更新——**不覆盖**，避免丢失 SullyOS 自己的修改 |

**总：15 个 utils/worker 文件 + 1 个 npm 包 = +3,100 行新增**

## 摸底

| 指标 | 第 1 步结束（`34a3a5e1`）| 第 1 步补完（本次）| 变化 |
|---|---|---|---|
| **TS 错误数** | 507 | 518 | +11 |
| **30+11+4 新文件引起的错误** | 109 | 94 | -15（realtimeFetchCore 解决了 agenticTools 的部分错误）|
| **Vite build** | ✅ 3.99s | ✅（30 个文件没被引用，Vite 不编译）| — |

## 剩余 94 错误的类型（这些是第 2 步要修的）

`30+11+4 个新文件` 还在调**第 2 步**才补的 API：

1. **ActiveMsgClient 上游新方法**（SullyOS 现有 activeMsgClient 没有）：
   - `deleteLlmCredentials` / `clearCharClientState` / `clearClientStateValue`（被 amsg2CharCleanup / amsg2StateSync 调）
   - 等等

2. **ActiveMsg2CharacterConfig 上游新字段**（SullyOS types.ts 没有）：
   - `tasks: ActiveMsg2TaskRecord[]`（被 amsg2DebugView 读）
   - `workerUrl: string`（被 amsg2CharCleanup 读）

3. **activeMsgStore 上游新方法**：
   - `upsertExpiredNotices`（被 amsg2TaskContext 调）
   - 等等

4. **ActiveMsg2GlobalConfig 上游新字段**：
   - `workerUrl` / `instantChatEnabled`（被 amsg2CharCleanup / amsgInstantChat 读）

**这 94 错误**都是**第 2 步**（扩展 3 个 SullyOS 已有 utils + types.ts 补字段）要修的。

## 状态判断

### 已闭环
- ✅ 30 个新文件就位（commit `34a3a5e1`）
- ✅ 11 个 SullyOS 缺依赖 + 4 个二次依赖就位（本次 commit）
- ✅ 装 `@capacitor/push-notifications@^6.0.5`（兼容 SullyOS Capacitor 6 生态）
- ✅ Vite build 通过

### 还没闭环
- ❌ 94 个新文件 TS 错误（**第 2 步**要修）
- ❌ **30+11+4 个新文件当前没被任何 modal 引用**——等第 3 步 modal 重写后才有 UI 入口

### SullyOS 没动什么
- 1.x 主动消息 / 9-6 WebView / Android 保活 / 现有 3 个 utils / 现有 2 个 modal / Worker 端 — 全部保留
- 41 个上游测试文件 — 不搬

## 下一步

**第 2 步：扩展 3 个 SullyOS 已有 utils（activeMsgClient / Runtime / Store）到 upstream 行数** + 补 `ActiveMsg2CharacterConfig.tasks` 字段 + 适配 SullyOS 的 8 个 SullyOS 方法（onSave plain object 等）

预计 1-2 小时
