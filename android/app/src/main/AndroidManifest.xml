<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- 暮色 2026-08-27 后台保活：轻量前台服务，常驻一条"运行中"通知，
             防止系统把 WebView 进程杀掉（消息推送 / 主动唤醒依赖进程存活） -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Android 14+ 用 specialUse 类型必须声明这个 property 说明用途 -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="保活 WebView 进程以维持实时消息推送和主动唤醒" />
        </service>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 暮色 2026-08-26 角色查手机 P0：
         PACKAGE_USAGE_STATS 是特殊权限（不是 runtime / dangerous / normal），
         加在 manifest 只是声明意图 + 让 Play Store 过滤，真授权要用户去设置页手动开
         tools:ignore="ProtectedPermissions" 抑制 lint 警告 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- 暮色 2026-08-27 后台保活（P0 第 5 项）：
         FOREGROUND_SERVICE — 允许起前台服务（Android 9+ 必须声明）
         FOREGROUND_SERVICE_SPECIAL_USE — Android 14+ 要求 FGS 必须带类型，specialUse 是兜底类型 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
</manifest>
