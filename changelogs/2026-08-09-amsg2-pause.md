# 主动消息 2.0 隔离(第一步)

**日期**:2026-08-09
**涉及 commit**:(待 push 后回填)

## 改了什么

暂停主动消息 2.0 全部运行入口,1.0 保持唯一触发通道。代码/配置/组件**全部保留**,切换开关在 `AMSG2_ENABLED`(单点常量),后续要重新启用 2.0 只改这一处 `false → true` 即可。

- 新增 `utils/activeMsgFeatureFlag.ts`:`export const AMSG2_ENABLED = false`
- `utils/proactiveChat.ts` 4 处 2.0 接入点短路(`start` / `markUserContact` / `stop` / `resume` 的 `registerScheduleOnWorker` + `unregisterScheduleOnWorker` 调用)
- `context/OSContext.tsx` 1.0 不动;2.0 的 `active-msg-received` useEffect body 开头加 `if (!AMSG2_ENABLED) return;`(`ActiveMsgRuntime.init` 也不调)
- `apps/Settings.tsx` 2.0 配置入口 section + `ActiveMsgGlobalSettingsModal` 弹窗,用 `{AMSG2_ENABLED && (...)}` 包(`useState` + `import` 保留)

## 动了哪些文件

- `utils/activeMsgFeatureFlag.ts` —— 新增(15 行,1 个常量)
- `utils/proactiveChat.ts` —— 加 1 个 import + 4 处 `if (AMSG2_ENABLED && isPushConfigReady(...))` 前置短路
- `context/OSContext.tsx` —— 加 1 个 import + 1 个 `if (!AMSG2_ENABLED) return;` 早退
- `apps/Settings.tsx` —— 加 1 个 import + 2 处 JSX `{AMSG2_ENABLED && (...)}` 包
- `changelogs/2026-08-09-amsg2-pause.md` —— 本文件

**未动**:
- 1.0 核心流程 `OSContext.runProactive`(line 1395-1620)
- 2.0 工具类 / Worker / push 客户端 / Settings 组件 / 配置文件
- Cloudflare 仪表盘 cron(暮色明天自己停)
- `max_tokens`(保持 2000)
- 工具调用能力(XIAO_ZHI_TIAO / 朋友圈 / 放歌 / 戳一戳)

## 踩坑

- `apps/Settings.tsx` 顶部 import 用 `replace_all` 时,`// 暮色 2026-08-06：主动消息 2.0 全局配置弹窗` 这行注释在文件里出现 2 次(顶部 + 函数体内),一次性替换把 import 误打到函数体里,build 报 `Unexpected "{"`。已修,只留顶部 line 20 的 import,函数体内 321 行那处删掉。

## 静态验证(部署前 grep 核对)

- `utils/proactiveChat.ts`:`AMSG2_ENABLED &&` 出现在 4 处接入点前(全部覆盖 register/unregister 调用)
- `context/OSContext.tsx`:`if (!AMSG2_ENABLED) return;` 在 2.0 useEffect body 第 1 行,`ActiveMsgRuntime.init` / `active-msg-received` listener 全部在该 useEffect body 内,均被短路
- `apps/Settings.tsx`:`{AMSG2_ENABLED && (...)}` 包住 2 处(入口 section + 弹窗),`showAmsg2Config` state 与 `import` 保留(仅不渲染)

`npm run build` 通过(4.03s,无 TypeScript 错误)。

## 运行时验证(待暮色在 Vercel preview 测)

预期:
1. Settings 页**无**"主动消息 2.0"配置入口卡片
2. 触发主动消息时,`localStorage['sullyos:lastProactiveReqLog']` 走 1.0 通道(`protocol: 'openai'`,`url: ${api.baseUrl}/chat/completions`)
3. 控制台**无** `active-msg-received` 事件 trace / `ActiveMsgRuntime.init()` 调用

## 备注

- 第二步(1.0 核心流程恢复 + wakeupId 硬去重 + 用户正在聊天不触发 + 删 8-6 之后上下文改动)**今天不执行**,等暮色批准
- Cloudflare 2 个 worker cron(amsg + proactive-push)仍**未停**,暮色自己停。本地代码短路**足够**让 2.0 不触发(worker cron 仍会发 Web Push,但前端 `active-msg-received` listener 已被 `AMSG2_ENABLED` 短路)
- 后续要重新启用 2.0:`utils/activeMsgFeatureFlag.ts` 改 `export const AMSG2_ENABLED = true` 即可,4 处调用点不需改
