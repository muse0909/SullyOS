## sync 2.0 step 3：整文件覆盖两个 modal + 接入 Amsg2DebugPanel

按暮色 9-15 11:51 拍板 1-2 一起做，把 upstream 主动消息 2.0 的两个 modal
和调试面板完整搬过来。

**1. `components/settings/ActiveMsgGlobalSettingsModal.tsx`：285 → 1741 行**
- 整文件从 upstream/master HEAD (27987fbb) 复制
- Cloudflare 一键部署面板 + VAPID 生成 + Worker 自更新 + 诊断面板 + 推送深度重置
  + Cron 暂停 + 手动部署 Worker 备份入口 + Deno Proxy 等所有上游功能
- 修了 3 处 SullyOS 适配：
  - 加 `ActiveMsg2DbDriver` import
  - `patchConfig` fallback 补 `driver: 'pg'`（SullyOS 类型 required）
  - apps/Settings.tsx 调用处补 `realtimeConfig` prop（d4234213 那次加的 prop）

**2. `components/chat/ActiveMsg2SettingsModal.tsx`：367 → 762 行**
- 整文件从 upstream/master 复制
- 多任务清单（tasks 数组）+ 即时对话开关 + 远端对账 + 任务编辑器等
- 修了 4 处 SullyOS 适配：
  - types.ts 加 `instantChatEnabled?: boolean` 字段（ActiveMsg2CharacterConfig）
  - `buildConfig` 补 `mode / firstSendTime / recurrenceType` 必填字段
  - handleToggleInstantChat + line 225 的 fallback 补 mode/firstSendTime/recurrenceType 必填
  - apps/Chat.tsx 调用处改 onSave 为 functional updater 形式：
    `(updater) => updateCharacter(char.id, { activeMsg2Config: updater(char.activeMsg2Config) })`
    （upstream 关键设计：避免面板保存期间远端新任务被旧快照盖掉）

**3. `App.tsx`：接入 Amsg2DebugPanel**
- 加 `import Amsg2DebugPanel from './components/Amsg2DebugPanel';`
- 在 `<OSProvider><MusicProvider>` 内挂 `<Amsg2DebugPanel />`（upstream App.tsx:45 同款）
- 注释说明 portal 渲染绕开 fixed 定位问题

**4. `types.ts`：补 1 个字段**
- `ActiveMsg2CharacterConfig.instantChatEnabled?: boolean`
  （注释按 SullyOS 风格标 "麦麦 2026-09-15 同步上游"）

**摸底：**
- Vite build ✓ 4.46s → 4.60s（稳定通过）
- tsc 错数 655 → 642（本次净减 13 个错）
- 22 个新文件相关错：仍是 0（modal 内部相关错已清零；amsg2TaskContext / amsg2ToolBridge / eventBox 几个错是 baseline 残留，跟本次同步无关）

**配色：**保留 SullyOS 9-11 浅紫 violet-300 风格（暮色 9-11 拍的），覆盖时核对没破坏。

**Cloudflare 一键部署能否跑：**UI 入口已通。`provisionAmsgBackend` 调的是用户的 Cloudflare 账号 + 中心代理 worker，运行时需要用户填 CF Token + 决定子域名。完整跑通要等 plan step 4（Worker 端 worker/amsg/src/index.ts）做完——但 plan 是"3 先暂放"。

**未触动：**
- Worker 端 `worker/amsg/src/index.ts`（plan step 4 暂放）
- Android 端 UnifiedPush gradle + 接收器（plan step 5 暂放）
- `components/DevDebugPanel.tsx`（upstream 有，SullyOS 没；不影响 Amsg2DebugPanel 接入）
- cfProvision.test.ts / vapidGen 测试（按 plan 不搬测试）

**下一步：**等暮色在 preview 验证（打开系统设置里的「主动消息 2.0」入口 + 调试面板 Amsg2DebugPanel）——确认整页没炸、Cloudflare 一键部署面板显示正常就 OK。