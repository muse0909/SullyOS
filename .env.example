# .env.example — 复制为 .env 填真值
#
# ⚠️ .env 默认在 .gitignore 里，不会提交到 git。
#    敏感值（CLIENT_TOKEN 等）绝不要写进 .env.example 或源代码。
#
# 字段说明（跟 utils/proactivePushConfig.ts 里 readEnv 的 key 一一对应）：
#   VITE_PROACTIVE_WORKER_URL     — Cloudflare Worker 域名 + /api 路径
#                                   例：https://proactive-push.你的子域.workers.dev
#   VITE_PROACTIVE_VAPID_PUBLIC_KEY — VAPID 公钥（公开安全值，理论上可不填；填了完整）
#   VITE_PROACTIVE_CLIENT_TOKEN   — 跟 Worker 的 CLIENT_TOKEN 一致（共享密钥）
#
# 跟 android/local.properties 的 KEEP_ALIVE_* 字段**不同**：
#   - VITE_PROACTIVE_* = 前端 Web Push 通道（SW 收 push → 主线程 runProactive）
#   - KEEP_ALIVE_*     = Android WebSocket 通道（KeepAliveService 收 proactive_message）
# 两条独立通道，可以都开、可以只开一条、都开占位符等于都没开。
#
# 占位符 = 不填任何值，所有函数 no-op，App 不会报错，只会本地兜底走 setInterval 路径。

# VITE_PROACTIVE_WORKER_URL=https://proactive-push.qegj567.workers.dev
# VITE_PROACTIVE_VAPID_PUBLIC_KEY=
# VITE_PROACTIVE_CLIENT_TOKEN=
