## sync 2.0 step 3 prep：补 2 个 modal 重写必需的缺模块

按暮色 9-15 11:51 拍板 1-2 一起做（modal 重写 + Cloudflare 一键部署子模块），
先补 modal 重写必需的 2 个上游 utils。

**2 个 SullyOS 缺的上游文件整文件从 upstream/master HEAD (27987fbb) 复制：**

| 文件 | upstream 行数 | 干什么 |
|---|---|---|
| `utils/vapidGen.ts` | 50 | VAPID 密钥生成（Web Push 推送订阅用 ECDSA P-256 + bytesToBase64Url/base64UrlToBytes 工具）|
| `utils/cfProvision.ts` | 938 | Cloudflare 一键部署主体：验 token → 找账号 → 建 D1 → 确认 workers.dev 子域 → 拉 bundle → 上传 Worker（密钥/D1 绑定一次带齐）→ 加 cron → 开访问地址。导出 `provisionAmsgBackend / attachUpdateCapability / waitForWorkerReady / CfAccount / ProvisionProgress / AMSG_SCRIPT_NAME / AMSG_D1_NAME` |

**测试：**`utils/cfProvision.test.ts` (476 行) **不搬**——按 9-12 11:35 plan 表"测试不搬"原则，运行时手验。

**依赖链：**cfProvision → `utils/proxyWorker.ts` (getProxyWorkerUrl) + `utils/vapidGen.ts` (generateVapidKeyPair / generateClientToken)——SullyOS 都齐。

**摸底：**
- Vite build ✓ 4.45s
- tsc 错数 655（baseline）→ 655（加完两个文件）→ **+0 新错**（cfProvision/vapidGen 引入错 0）
- 22 个新文件相关错仍保持 0（grep `cfProvision|vapidGen` 0 命中）

**未触动的文件：**
- 现有 `ActiveMsgGlobalSettingsModal.tsx`（285 行 Neon 极简版）——下个 commit 整文件覆盖到 1741 行
- 现有 `ActiveMsg2SettingsModal.tsx`（367 行）——下个 commit 整文件覆盖到 762 行
- `App.tsx`（没接入 Amsg2DebugPanel）——再下个 commit 处理
- Worker 端 `worker/amsg/src/index.ts`（plan step 4 暂放）
- Android 端 UnifiedPush gradle 集成（plan step 5 暂放）

**下一步：step B 摸底 modal 重写依赖 + step C 整文件覆盖两个 modal + step D 接入 Amsg2DebugPanel。**